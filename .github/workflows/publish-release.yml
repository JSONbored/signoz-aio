name: Publish Release / SigNoz-AIO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish-release:
    uses: JSONbored/aio-fleet/.github/workflows/aio-publish-release.yml@ce6221adb01de3fbe16b40fa0274c950c1ccd225
    permissions:
      actions: read
      contents: write
    with:
      release_name: SigNoz-AIO
      component: signoz-aio
      workflow_selector: build.yml
    secrets: inherit
