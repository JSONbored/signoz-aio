name: Prepare Release / SigNoz-AIO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prepare-release:
    uses: JSONbored/aio-fleet/.github/workflows/aio-prepare-release.yml@6cb527a4cccd624ad73447b6b73cda9b56b4c9c7
    permissions:
      contents: write
      pull-requests: write
    with:
      release_name: SigNoz-AIO
      component: signoz-aio
      component_label: signoz-aio
      previous_tag_command: latest-aio-tag
    secrets: inherit
