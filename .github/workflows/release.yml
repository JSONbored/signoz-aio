name: Prepare Release / SigNoz-AIO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prepare-release:
    uses: JSONbored/aio-fleet/.github/workflows/aio-prepare-release.yml@5426c8638dc2bb2bf1faa355d9db786ad044c7fa
    permissions:
      contents: write
      pull-requests: write
    with:
      release_name: SigNoz-AIO
      component: signoz-aio
      component_label: signoz-aio
      previous_tag_command: latest-aio-tag
    secrets: inherit
