name: Prepare Release / SigNoz-AIO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prepare-release:
    uses: JSONbored/aio-fleet/.github/workflows/aio-prepare-release.yml@6f895a48251e0478918664968b19542b85056ed2
    permissions:
      contents: write
      pull-requests: write
    with:
      release_name: SigNoz-AIO
      component: signoz-aio
      component_label: signoz-aio
      previous_tag_command: latest-aio-tag
    secrets: inherit
