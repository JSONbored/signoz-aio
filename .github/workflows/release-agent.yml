name: Prepare Release / SigNoz Agent

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prepare-release:
    uses: JSONbored/aio-fleet/.github/workflows/aio-prepare-release.yml@4caf10de3d95ab99b67f65766cd52dd80cb7f75c
    permissions:
      contents: write
      pull-requests: write
    with:
      release_name: SigNoz Agent
      component: signoz-agent
      component_label: signoz-agent
      previous_tag_command: latest-aio-tag
    secrets: inherit
